//making a function that converts string into capital case
function capitalCase(str) {
    str = str.toUpperCase();
    alert("string is in upper case" + str)

}
var str = prompt("enter a string");
capitalCase(str);